#В комиссии К членов выбирается председатель и заместитель. Вывести все возможные варианты. разработать реализацию с использованием графического интерфейса. Допускается использовать любую графическую библиотеку питона. Рекомендуется использовать внутреннюю библиотеку питона tkinter. В программе должны быть реализованы минимум одно окно ввода, одно окно вывода, текстовое поле, кнопка
import tkinter as tk

def calculate():
    K = int(entry.get())
    result = []
    for i in range(1, K+1):
        for j in range(1, K+1):
            if i != j:
                result.append("Председатель: " + str(i) + ", Заместитель: " + str(j))
    output.delete(1.0, tk.END)
    output.insert(tk.END, "\n".join(result))

root = tk.Tk()
root.title("Выбор председателя и заместителя комиссии")

# Создаем окно ввода
entry_label = tk.Label(root, text="Введите количество членов комиссии:")
entry_label.pack()

entry = tk.Entry(root)
entry.pack()

# Создаем кнопку "Рассчитать"
calculate_button = tk.Button(root, text="Рассчитать", command=calculate)
calculate_button.pack()

# Создаем окно вывода
output_label = tk.Label(root, text="Результат:")
output_label.pack()

output = tk.Text(root, height=10, width=40)
output.pack()

root.mainloop()
